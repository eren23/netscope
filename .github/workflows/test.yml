name: tests

on:
  push:
    branches: [main]
  pull_request:

jobs:
  python:
    name: pytest (py${{ matrix.python-version }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.9", "3.10", "3.11", "3.12"]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
      - name: Install (CPU torch + torchvision + transformers + dev)
        run: |
          python -m pip install --upgrade pip
          pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
          pip install transformers
          pip install -e ".[dev]"
      - name: Run the suite
        run: pytest tests/ -q

  extension:
    name: extension (tsc + unit + headless)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Provision .venv for the headless integration tests
        # headless.js shells out to <repo>/.venv/bin/python and executes the demo
        # models (examples/sfumato_cmajc.py, mismatch_demo.py), which need torch +
        # netscope. Provide exactly that venv so the real run -> webview path runs.
        run: |
          python -m venv .venv
          .venv/bin/python -m pip install --upgrade pip
          .venv/bin/pip install torch --index-url https://download.pytorch.org/whl/cpu
          .venv/bin/pip install -e .
      - name: Build + test the extension
        working-directory: extension
        run: |
          npm install
          npm run compile
          npm run test:unit
          npm run test:headless

  build:
    name: build + twine check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Build the wheel + sdist and check them
        run: |
          python -m pip install --upgrade pip build twine
          python -m build
          python -m twine check dist/*
